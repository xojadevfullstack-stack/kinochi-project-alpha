"""
Core layer — settings, config, security utilities, logging.

This package holds cross-cutting concerns that every other layer may
import, but it never imports from domain, application, infrastructure,
or api packages.
"""
